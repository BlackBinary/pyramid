mutation CreateStrategy($title: String!, $description: String) {
    createStrategy(title: $title, description: $description) {
        id
        title
        description
    }
}
mutation DeleteStrategy($id: ID!) {
    deleteStrategy(id: $id) {
        id
    }
}